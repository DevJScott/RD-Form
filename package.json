{
  "name": "rd-claim-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-all": "npm --prefix client install && npm --prefix server install",
    "build-client": "npm --prefix client run build",
    "start": "npm run build-client && node server/index.js",
    "dev": "npm run build-client && nodemon server/index.js"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.16.3"
  }
}
